import 'package:flutter/material.dart';

class DeepCleanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Deep Clean")),
      body: Center(child: Text("Welcome to Deep Clean!")),
    );
  }
}
